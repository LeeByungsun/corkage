# Corkage Project AGENTS

## 목적

이 저장소는 `콜키지 가능한 식당을 웹에서 보여주는 서비스`를 만들기 위한 프로젝트 전용 작업 규칙입니다.

현재 단계는 개발보다 아래가 우선입니다.

1. 네이버 지도/네이버 API 사용 가능 여부 검증
2. 콜키지 데이터 확보 방식 검증
3. 서비스 기획 정리

## 기본 원칙

- 섣불리 구현부터 시작하지 않는다.
- 먼저 `API로 가능한 것`과 `API로 안 되는 것`을 분리한다.
- 콜키지 정보는 외부 API만으로 완성되지 않는다고 가정하고 검증한다.
- 작은 문서 단위로 결정 사항을 남긴다.
- 실제 개발은 검증 체크리스트가 통과된 뒤에 시작한다.

## 현재 권장 작업 순서

1. `docs/api-feasibility-and-product-plan.md` 갱신
2. API 테스트 항목 확정
3. 결과 기록용 문서 또는 스크립트 추가
4. MVP 데이터 정책 확정
5. 이후에만 앱 구조 설계 및 구현

## 권장 에이전트

사전 검증/기획 단계에서는 아래 에이전트를 우선 사용합니다.

- `researcher`:
  네이버 공식 문서, 정책, 호출 조건 확인
- `architect`:
  데이터 흐름과 MVP 구조 설계
- `planner`:
  API 테스트 순서와 산출물 정리
- `designer`:
  사용자 흐름과 지도 UX 정의
- `verifier`:
  API 가능 여부와 가설 검증
- `writer`:
  결정사항 문서화

구현 단계로 넘어간 뒤에야 아래를 본격 사용합니다.

- `executor`
- `debugger`
- `test-engineer`
- `code-reviewer`

## 권장 스킬

현재 단계 핵심 스킬:

- `plan`
- `ralplan`
- `analyze`
- `note`
- `wiki`

조건부 사용:

- `deep-interview`:
  요구사항이 넓거나 아직 서비스 범위가 모호할 때
- `code-review`:
  구현이 시작된 뒤
- `security-review`:
  인증키, 서버 프록시, 개인정보 처리가 들어갈 때

## 완료 기준

아래가 모두 채워지기 전에는 구현 완료라고 보지 않습니다.

- 어떤 네이버 API를 쓸지 결정됨
- 브라우저 직접 호출과 서버 경유 호출 경계가 정리됨
- 콜키지 데이터의 출처와 갱신 방식이 정해짐
- MVP 화면 목록이 정해짐
- 검색/필터 핵심 정책이 정해짐
